# Create the conda environment and run these commands in the terminal to install the dependencies for the project:

conda create -n hyperspectral_env python=3.8
conda activate hyperspectral_env
pip install -r Requirements.txt


# Install these dependencies in the conda environment:

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.23.5
scikit-learn>=1.2.0
scipy>=1.10.1
matplotlib>=3.6.3
spectral>=0.23.1

